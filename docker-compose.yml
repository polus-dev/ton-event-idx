version: '3.8'

services:
  main:
    container_name: ton-event-idx-dev
    hostname: ton-event-idx-dev
    build:
      dockerfile: ./docker/Dockerfile
      target: dev
      context: .
    volumes:
      - .:/app/
    env_file:
      - .env

networks:
  default: